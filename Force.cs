using System.Numerics;

namespace Physics
{
    internal class Force
    {

        public Vector2 GenerateDragForce(Particle particle, float k)
        {
            return new Vector2(0, 0);
        }
    }
}
